const baseURL = process.env.REACT_APP_API;

const mathOperationsList = 'math-operations/list';
const calculateExpression = 'expression';
const history = 'history';

const urls = {
    mathOperationsList,
    calculateExpression,
    history,
}

export {
    baseURL,
    urls,
}